# Café Luna – Local Café Website

**Live Demo:** https://cafe-luna-liard.vercel.app/

## Overview

Final project for **ACIT 1620 – Fundamental Web Technologies**.  
A cozy café website built using **semantic HTML** and **external CSS**, following proper layout, color harmony, and responsive design practices.

---

## 🚀 Workflow (IMPORTANT)

### 1. Clone the repo

```bash
git clone https://github.com/ubcyukiny/cafe-luna.git
cd cafe-luna
```

### 2. Create a new branch (DO NOT work on main)

```bash
git checkout -b feature-branch-name
git push --set-upstream origin feature-branch-name
```

### 3. Pull before working (to stay up-to-date)

```bash
git pull origin main
```

### 4. Commit + Push

```bash
git add .
git commit -m "your message"
git push
```

### 5. Open a Pull Request (PR)

- GitHub → **Pull Requests → New PR**
- **Base:** main
- **Compare:** your branch
- Assign **Ken** as reviewer
- ❌ **Do NOT merge your own PR**
- ❌ **Do NOT push to main**

Work ONLY on your feature branch.

---

## 🌟 Live Deployment

Our site is deployed on **Vercel**:  
👉 https://cafe-luna-liard.vercel.app/

Every push to `main` redeploys automatically.

---

## 👥 Work Distribution

### Ken

- Final global polish (spacing, colors, typography consistency)
- Responsive fixes (mobile/tablet)
- Final layout adjustments across all pages
- Navbar + footer (added at the very end)
- Code review + merge PRs
- Minor animations / hover effects / cleanup

---

### Luke — Home Page (`index.html`)

- Build full home page HTML
- Hero section layout + text
- Daily special block
- Insert homepage images
- Basic styling in their own section of CSS

---

### Vincent — Menu Page (`menu.html`)

- Build full menu page HTML
- Create menu item cards (image, name, price)
- Organize items in a simple grid
- Add menu photos
- Write item descriptions

---

### Christian — Figma design, About Page (`about.html`)

- Build figma design file
- Build full about page HTML
- Café story section
- Opening hours table
- Add café image
- Add simple map placeholder

---

## 📁 File Structure

```
cafe-luna/
├── index.html
├── menu.html
├── about.html
├── css/
│   ├── style.css
│   ├── index.css
│   ├── menu.css
│   └── about.css
└── images/
```

---

## Setup

1. Open the folder in VS Code.
2. Edit each HTML file and `css/style.css`.
3. Add café images inside `/images`.
4. Test layout on desktop + mobile.

## Submission

Compress and submit as `CafeLuna_SetLetter_GroupNumber.zip`.
