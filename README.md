# Café Luna – Local Café Website

## Overview
Starter project for **ACIT 1620 – Fundamental Web Technologies**.  
Build a cozy café website using **HTML + CSS**.

---

## 🚀 Workflow (IMPORTANT)

### 1. Clone the repo
```bash
git clone https://github.com/ubcyukiny/cafe-luna.git
cd cafe-luna
```

### 2. Create a new branch (DO NOT work on main)
```bash
git checkout -b feature-branch-name
git push --set-upstream origin feature-branch-name
```

### 3. Pull before working (to stay up-to-date)
```bash
git pull origin main
```

### 4. Commit + Push
```bash
git add .
git commit -m "your message"
git push
```

### 5. Open a Pull Request (PR)
- GitHub → **Pull Requests → New PR**  
- **Base:** main  
- **Compare:** your branch  
- Assign **Ken** as reviewer  
- ❌ **Do NOT merge your own PR**  
- ❌ **Do NOT push to main**  

Work ONLY on your feature branch.

---

## 👥 Work Distribution

### Ken
- Global CSS (colors, fonts, spacing, layout)
- Navbar + footer
- Page structure & containers
- Responsive design (mobile/tablet)
- Grid/Flex system for all pages
- Code review + merge PRs
- Final visual polish (spacing, animations, cleanup)

### Luke — Home Page (`index.html`)
- Hero section content (heading, tagline)
- Daily special section
- Insert images + text

### Vicent — Menu Page (`menu.html`)
- Menu item cards (image, name, description, price)
- Organize items using Ken’s grid
- Add menu images
- Content only, no custom CSS

### Christian — About Page (`about.html`)
- Café story section
- Opening hours table
- Image + map placeholder
- Simple layout using existing classes


---

## 📁 File Structure
```
cafe-luna/
├── index.html
├── menu.html
├── about.html
├── css/
│   └── style.css
└── images/
```

---

## Setup
1. Open the folder in VS Code.  
2. Edit each HTML file and `css/style.css`.  
3. Add café images inside `/images`.  
4. Test layout on desktop + mobile.



## Submission
Compress and submit as `CafeLuna_SetLetter_GroupNumber.zip`.
